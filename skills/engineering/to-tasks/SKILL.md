---
name: to-tasks
description: Break a plan, spec, or the current conversation into a set of tracer-bullet tasks, each declaring its blocking edges, published to the configured tracker (edges as text in one file per task locally, or native blocking links on a real tracker).
disable-model-invocation: true
---

Call the Skill tool with "splitting-tasks".

If the user passed a reference (a spec path, an issue number or URL) as an argument, hand it through.
